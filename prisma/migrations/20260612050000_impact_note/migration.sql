-- Explicación clicleable del impacto cultural: por qué este disco tiene esta
-- nota, narrada SOLO sobre hechos verificados del FactsPayload (premios,
-- certificaciones, listas tipo Rolling Stone) y pasada por el verificador.
ALTER TABLE "Dossier" ADD COLUMN "impactNote" TEXT;
